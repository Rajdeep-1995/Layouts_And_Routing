<div>
    <h2>@ViewBag.name</h2>
    @{ 
        <ul>
            @for (int i = 0; i < ViewBag.items.Count; i++)
            {
            <li>@ViewBag.items[i]</li>
            }
        </ul>
    }
</div>
